---
# Install and configure Docker engine

- name: Ensure required packages for Docker repo management are present (Debian/Ubuntu)
  ansible.builtin.apt:
    name:
      - ca-certificates
      - curl
      - gnupg
      - lsb-release
    state: present
    update_cache: true
  when: ansible_os_family == 'Debian'
  tags: [docker_setup]

- name: Create directory for Docker GPG key (Debian/Ubuntu)
  ansible.builtin.file:
    path: /etc/apt/keyrings
    state: directory
    mode: '0755'
  when: ansible_os_family == 'Debian'
  tags: [docker_setup]

- name: Add Docker's official GPG key (Debian/Ubuntu)
  ansible.builtin.get_url:
    url: https://download.docker.com/linux/ubuntu/gpg
    dest: /etc/apt/keyrings/docker.asc
    mode: '0644'
  when: ansible_os_family == 'Debian'
  tags: [docker_setup]

- name: Add Docker repository (Debian/Ubuntu)
  ansible.builtin.apt_repository:
    repo: "deb [arch={{ 'x86_64' if ansible_architecture == 'x86_64' else ansible_architecture }} signed-by=/etc/apt/keyrings/docker.asc] https://download.docker.com/linux/{{ ansible_distribution | lower }} {{ ansible_distribution_release }} {{ docker_setup_repo_channel }}"
    state: present
    filename: docker
  when: ansible_os_family == 'Debian'
  tags: [docker_setup]

- name: Update apt cache after adding Docker repository (Debian/Ubuntu)
  ansible.builtin.apt:
    update_cache: true
  when: ansible_os_family == 'Debian'
  tags: [docker_setup]

- name: Install Docker packages (Debian/Ubuntu)
  ansible.builtin.apt:
    name: "{{ docker_setup_packages }}"
    state: present
  when: ansible_os_family == 'Debian' and docker_setup_install | bool
  tags: [docker_setup]

- name: Ensure docker service is running
  ansible.builtin.service:
    name: "{{ docker_setup_service_name }}"
    state: started
    enabled: true
  when: docker_setup_install | bool
  tags: [docker_setup]

- name: Create Docker data directory when data-root is set
  ansible.builtin.file:
    path: "{{ docker_setup_data_root }}"
    state: directory
    mode: '0711'
  when: docker_setup_data_root | length > 0
  tags: [docker_setup]

- name: Render Docker daemon.json (data-root and extra)
  ansible.builtin.template:
    src: daemon.json.j2
    dest: /etc/docker/daemon.json
    mode: '0644'
  notify: docker_setup_restart
  when: docker_setup_data_root | length > 0 or (docker_setup_daemon_extra | length > 0)
  tags: [docker_setup]

- name: Add users to docker group
  ansible.builtin.user:
    name: "{{ docker_setup_user }}"
    groups: docker
    append: true
  loop: "{{ docker_setup_users }}"
  loop_control:
    loop_var: docker_setup_user
  when: docker_setup_users | length > 0
  tags: [docker_setup]

# TODO: Add RedHat family support if/when needed.
