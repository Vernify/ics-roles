{#
  Renders /etc/docker/daemon.json merging optional data-root and extra settings
#}
{% set base = {} %}
{% if docker_runtime_data_root | length > 0 %}
{% set base = base | combine({'data-root': docker_runtime_data_root}) %}
{% endif %}
{{ (base | combine(docker_runtime_daemon_extra)) | to_nice_json }}
